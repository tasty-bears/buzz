<%= partial('layout_header'); %>
  <body>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="i-bar"><i class="icon-chevron-down icon-white"></i></span>
          </a>
          <%- linkTo('Angora', mainsPath, {class: 'brand'}) %>
          <div class="nav-collapse">
            <ul class="nav">
              <li><%- linkTo('Courses', '/courses') %></li>
              <li><%- linkTo('Events', '/events') %></li>
            </ul>
          </div><!--/.nav-collapse -->
          <div class="nav-collapse pull-right">
            <ul class="nav">
            <% if (user) { %>
              <li><%- linkTo(user.givenName, userPath(user.id)); %><li>
              <li><%- linkTo('Log out', logoutPath) %></li>
            <% } else { %>
              <li><%- linkTo('Log in', loginPath) %></li>
            <% } %>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <div class="container">
      <%- displayFlash(flash); %>
      <%- render(); %>
    </div>
  </body>
<%= partial('layout_footer'); %>

